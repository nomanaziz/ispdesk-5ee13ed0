// Thin wrapper — reuses the existing reseller invoices page which already
// works for bw_customer (it keys off getBillingCustomerId).
import ResellerInvoices from "@/pages/reseller/ResellerInvoices";
export default ResellerInvoices;
